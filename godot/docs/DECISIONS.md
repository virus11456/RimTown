# 決策紀錄

- D-01 已確認（使用者）：先做 Phase 0–3 觀賞版，美術驗收後才開始 4–6。
- D-02 已確認：2D／3D 並行，未來 `/3d/`，由擁有者部署。
- D-03 已確認：首個可玩版先邊境鎮，海風鎮資產先備妥；觀赏讀檔可切兩鎮。
- D-04 已確認：預渲染 OGG；音樂屬後续 Phase 6，本階段不生成。
- D-05 已確認：四向正交相機；村民含年齡／性格外觀差異。
- D-06 測試預設：seed=11456；VM Math.random 使用原 SeededRandom 同式 Park–Miller。固定 epoch，每 tick 加 2000ms，微任務每 tick 清空。不連 AI，不修改原 JS。
- D-07 待確認：尺寸衝突採相容性优先：2×2 基本小屋資產，在世界按原 tilemap footprint 縮放。原碼住宅 6×6，不擅自搬動道路或住房。
- D-08 待確認：因原存檔不含連續位置，觀賞版重建 currentLocation 對應位置；無法保證與任意遊玩中 2D 截圖中的行走位置一致。驗收改採同存檔重新載入之格網／地點／人數／門口。
- D-09 工程預設：觀赏模式唯讀，JSON 全量保留並可下載副本，無雲端寫入按鈕，tickCount 不變。ApiClient 提供後續 write API，但預設 read_only=true，stale 不能視為成功覆寫。
- D-10 待確認：正式帳號由擁有者在 UI 自行登入驗收；開發不建立正式帳號或索取密碼。使用本機 HTTP 契約測試驗證請求與回應，實際登入驗收須單獨標記。
